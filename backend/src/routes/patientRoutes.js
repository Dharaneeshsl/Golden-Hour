const express=require("express");const{requireAuth}=require("../middleware/authMiddleware");module.exports=c=>{const r=express.Router();r.post("/",requireAuth,c.create);r.get("/:id",requireAuth,c.get);return r};