require("dotenv").config();const express=require("express"),cors=require("cors"),helmet=require("helmet"),rateLimit=require("express-rate-limit"),crypto=require("crypto");const{createStore}=require("./config/db");const{ChainService}=require("./services/chainService");const encryption=require("./services/encryptionService"),ipfs=require("./services/ipfsService");const{createPatientController}=require("./controllers/patientController"),{createDoctorController}=require("./controllers/doctorController"),{createRecordController}=require("./controllers/recordController"),{createEmergencyController}=require("./controllers/emergencyController");const patientRoutes=require("./routes/patientRoutes"),doctorRoutes=require("./routes/doctorRoutes"),recordRoutes=require("./routes/recordRoutes"),emergencyRoutes=require("./routes/emergencyRoutes"),authRoutes=require("./routes/authRoutes");async function createApp({store,chain=new ChainService()}={}){store=store||await createStore();const app=express();app.disable("x-powered-by");app.use((q,s,n)=>{q.requestId=crypto.randomUUID();s.setHeader("X-Request-Id",q.requestId);n()});app.use(helmet(),cors({origin:process.env.FRONTEND_ORIGIN||"http://localhost:5173",credentials:true}),express.json({limit:"256kb"}));app.use("/api",rateLimit({windowMs:900000,max:300}));const c={store,chain,encryption,ipfs};app.get("/health",(_q,s)=>s.json({status:"ok"}));app.get("/ready",async(_q,s)=>{try{if(store.query)await store.query("select 1");s.json({status:"ready",database:process.env.DATABASE_URL?"postgres":"local"})}catch(e){s.status(503).json({status:"not_ready"})}});app.use("/api/auth",authRoutes());app.use("/api/patients",patientRoutes(createPatientController(c)));app.use("/api/doctors",doctorRoutes(createDoctorController(c)));app.use("/api/records",recordRoutes(createRecordController(c),store));app.use("/api/emergency-access",emergencyRoutes(createEmergencyController(c),store));app.use((e,q,s,n)=>s.status(500).json({error:"Internal server error",requestId:q.requestId}));return app}if(require.main===module)createApp().then(a=>a.listen(process.env.PORT||4000));module.exports={createApp};