const jwt=require("jsonwebtoken");
function requireAuth(req,res,next){ const token=(req.headers.authorization||"").replace(/^Bearer\s+/i,""); if(!token) return res.status(401).json({error:"Authentication required"}); try { const secret=process.env.JWT_SECRET; if(!secret||secret==="local-development-secret") throw new Error("JWT secret unavailable"); req.user=jwt.verify(token,secret,{issuer:"goldenhour"}); next(); } catch { return res.status(401).json({error:"Invalid or expired authentication token"}); } }
module.exports={requireAuth};