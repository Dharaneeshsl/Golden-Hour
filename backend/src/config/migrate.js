require("dotenv").config();const fs=require("fs"),path=require("path"),{Pool}=require("pg");if(!process.env.DATABASE_URL)throw new Error("DATABASE_URL required");(async()=>{const pool=new Pool({connectionString:process.env.DATABASE_URL});await pool.query(fs.readFileSync(path.join(__dirname,"../../migrations/001_initial.sql"),"utf8"));await pool.end();console.log("Migration complete")})().catch(e=>{console.error(e);process.exit(1)});