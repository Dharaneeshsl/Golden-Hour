require("dotenv").config();const fs=require("fs"),path=require("path"),{Pool}=require("pg");if(!process.env.DATABASE_URL)throw new Error("DATABASE_URL required");(async()=>{const pool=new Pool({connectionString:process.env.DATABASE_URL,ssl:process.env.DB_SSL==="true"?{rejectUnauthorized:false}:undefined});for(const file of ["001_initial.sql","002_consents.sql"]){await pool.query(fs.readFileSync(path.join(__dirname,"../../migrations",file),"utf8"));}await pool.end();console.log("Migrations complete")})().catch(e=>{console.error(e);process.exit(1)});