const crypto=require("crypto");function createConsentController({store,chain}){return{grant:async(req,res)=>{const patient=await store.patientById(req.params.patientId);if(!patient)return res.status(404).json({error:"Patient not found"});if(req.user.role!=="admin"&&patient.wallet.toLowerCase()!==req.user.wallet.toLowerCase())return res.status(403).json({error:"Only the patient can grant consent"});if(req.user.role==="admin")return res.status(403).json({error:"Administrators have read-only consent oversight"});const provider=await store.providerByWallet(req.body.wallet);if(!provider||provider.status!=="verified")return res.status(400).json({error:"A verified provider wallet is required"});try{if(chain.enabled){const chainPatientId=await chain.patientId(patient.wallet);if(!chainPatientId)throw new Error("Patient is not registered on chain");await chain.grantAccess(patient.wallet,chainPatientId,provider.wallet,0)}const consent=await store.grantConsent({id:crypto.randomUUID(),patientId:patient.id,providerId:provider.id,scope:req.body.scope||["records"],grantedAt:new Date().toISOString()});await store.addAudit({id:crypto.randomUUID(),patientId:patient.id,actor:req.user.wallet,action:"CONSENT_GRANTED",providerId:provider.id,metadata:{scope:consent.scope},timestamp:new Date().toISOString()});res.status(201).json({...consent,providerWallet:provider.wallet,providerName:provider.name,providerSpecialty:provider.specialty})}catch(e){res.status(502).json({error:"Consent grant failed",details:e.message})}},revoke:async(req,res)=>{const patient=await store.patientById(req.params.patientId);if(!patient)return res.status(404).json({error:"Patient not found"});if(patient.wallet.toLowerCase()!==req.user.wallet.toLowerCase())return res.status(403).json({error:"Only the patient can revoke consent"});const provider=await store.providerByWallet(req.body.wallet);if(!provider)return res.status(404).json({error:"Provider not found"});try{if(chain.enabled){const chainPatientId=await chain.patientId(patient.wallet);if(!chainPatientId)throw new Error("Patient is not registered on chain");await chain.revokeAccess(patient.wallet,chainPatientId,provider.wallet)}const consent=await store.revokeConsent(patient.id,provider.id);if(!consent)return res.status(404).json({error:"Active consent not found"});await store.addAudit({id:crypto.randomUUID(),patientId:patient.id,actor:req.user.wallet,action:"CONSENT_REVOKED",providerId:provider.id,timestamp:new Date().toISOString()});res.json({...consent,providerWallet:provider.wallet,providerName:provider.name,providerSpecialty:provider.specialty})}catch(e){res.status(502).json({error:"Consent revoke failed",details:e.message})}},list:async(req,res)=>{const patient=await store.patientById(req.params.patientId);if(!patient)return res.status(404).json({error:"Patient not found"});if(req.user.role!=="admin"&&patient.wallet.toLowerCase()!==req.user.wallet.toLowerCase())return res.status(403).json({error:"Consent access denied"});res.json(await store.consentsForPatient(patient.id))}}}module.exports={createConsentController};