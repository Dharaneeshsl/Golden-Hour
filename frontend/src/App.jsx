import {useState} from "react";import {Link,Route,Routes,useNavigate,useParams,Navigate} from "react-router-dom";import PatientDashboard from "./components/PatientDashboard";import DoctorDashboard from "./components/DoctorDashboard";import EmergencyScanView from "./components/EmergencyScanView";import PatientOnboarding from "./components/PatientOnboarding";import AdminDashboard from "./components/AdminDashboard";import Login from "./pages/login";import WalletConnectButton from "./components/WalletConnectButton";import {setToken} from "./api";
function Home(){return <section className="hero"><div><p className="eyebrow">Every second matters</p><h1>Medical context when <em>minutes matter.</em></h1><p className="lead">Encrypted records, wallet-verified authentication, patient-controlled access and immutable audit trails.</p><div className="hero-actions"><Link className="primary" to="/login">Secure sign in</Link><Link className="secondary" to="/emergency">Emergency access</Link></div></div></section>}
function PatientRoute({user}){const{id}=useParams();if(!user)return <Navigate to="/login" replace/>;if(id==="me"&&!user.patientId)return <Navigate to="/onboarding" replace/>;return <PatientDashboard patientId={id==="me"?user.patientId:id} wallet={user.wallet}/>}
function EmergencyRoute(){const{id}=useParams();return <EmergencyScanView initialPatientId={id||""}/>}
export default function App(){const[user,setUser]=useState(()=>JSON.parse(localStorage.getItem("goldenhour_user")||"null")),navigate=useNavigate();const login=next=>{localStorage.setItem("goldenhour_user",JSON.stringify(next));setUser(next)};const complete=patient=>login({...user,patientId:patient.id});const logout=()=>{setToken(null);localStorage.removeItem("goldenhour_user");setUser(null);navigate("/")};return <div className="app"><header><Link className="brand" to="/"><span className="mark">GH</span><div><strong>GoldenHour</strong><small>Trusted emergency history</small></div></Link><nav>{user?.role==="admin"&&<Link to="/admin">Admin portal</Link>}{user&&user.role!=="admin"&&<Link to="/patient/me">Patient portal</Link>}{user&&user.role!=="admin"&&<Link to="/doctor">Provider portal</Link>}<Link to="/emergency">Emergency access</Link></nav>{user?<button className="wallet" onClick={logout}>Sign out</button>:<WalletConnectButton onConnected={()=>navigate("/login")}/>}</header><main><Routes><Route path="/" element={<Home/>}/><Route path="/login" element={<Login onLogin={login}/>}/><Route path="/onboarding" element={user?<PatientOnboarding onComplete={complete}/>:<Navigate to="/login"/>}/><Route path="/patient/:id" element={<PatientRoute user={user}/>}/><Route path="/doctor" element={user?<DoctorDashboard wallet={user.wallet}/>:<Navigate to="/login"/>}/><Route path="/admin" element={user?.role==="admin"?<AdminDashboard/>:<Navigate to="/login"/>}/><Route path="/emergency" element={<EmergencyRoute/>}/><Route path="/emergency/:id" element={<EmergencyRoute/>}/></Routes></main><footer><span>GoldenHour</span><span>Wallet verification • Encryption • Authenticated API</span></footer></div>}
