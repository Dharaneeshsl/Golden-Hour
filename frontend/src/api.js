const API_URL = import.meta.env.VITE_API_URL || "http://localhost:4000";
export function setToken(token) { if (token) localStorage.setItem("goldenhour_token", token); else localStorage.removeItem("goldenhour_token"); }
export function getToken() { return localStorage.getItem("goldenhour_token"); }
export async function request(path, options = {}) { const headers = { "Content-Type": "application/json", ...(options.headers || {}) }; const token = getToken(); if (token) headers.Authorization = `Bearer ${token}`; const response = await fetch(`${API_URL}${path}`, { ...options, headers }); const data = await response.json().catch(() => ({})); if (!response.ok) throw new Error(data.error || `Request failed: ${response.status}`); return data; }
export const api = { login: (body) => request("/api/auth/login", { method: "POST", body: JSON.stringify(body) }), patient: (id) => request(`/api/patients/${id}`), registerPatient: (body) => request("/api/patients", { method: "POST", body: JSON.stringify(body) }), records: (id) => request(`/api/records/${id}`), addRecord: (body) => request("/api/records", { method: "POST", body: JSON.stringify(body) }), emergency: (body) => request("/api/emergency-access", { method: "POST", body: JSON.stringify(body) }), audit: (id) => request(`/api/emergency-access/${id}`) };
